#include <vector>
#include <list>
using namespace std;

class HashLinear{
public:
	void print1(int);
	vector <int> search(int);
	void insert(int);
	HashLinear(int);
private:
	int p;
	vector <int>vect1;
};